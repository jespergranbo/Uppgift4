@model IEnumerable<Uppgift4.ViewModels.UserProgramVM>

@{
    ViewBag.Title = "LoggedIn";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>LoggedIn</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.user_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.channel_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.channel_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.user_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.channel_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.channel_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.user_channel_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.user_channel_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.user_channel_id })
        </td>
    </tr>
}

</table>
